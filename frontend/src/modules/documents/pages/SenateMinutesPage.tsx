import DocumentsPage from "./DocumentsPage";

export default function SenateMinutesPage() {
  return (
    <DocumentsPage
      documentType="senate"
      title="Senate Agenda"
      description="Browse senate agenda minutes by year. Ask questions scoped to senate documents only."
    />
  );
}
